spring:
  datasource:
    url: jdbc:mysql://localhost:3306/testdb
    password: Spring.Student@00
    username: ahmed2
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format-sql: true
  flyway:
    baseline-on-migrate: true
    enabled: true
    baseline-description: "init"
    baseline-version: 0
  h2:
    console:
      enabled: true